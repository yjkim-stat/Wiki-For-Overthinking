# Research digest — 2026-09-01

<!-- session:begin -->

## Session — 2026-09-01

Answered 84 task(s): 44 paper, 40 concept.

**Left for the next night**

- 3 paper task(s) waiting
- 37 concept task(s) waiting
- the oldest has been waiting since 2026-09-01
- 155 definition(s) their evidence has outgrown
- 4 finding(s) their subject has outgrown

<!-- session:end -->

Finished round 7 of the queue-drain that has been running since 2026-08-28. A
background agent doing round 7 was cut off mid-way by hitting the monthly
spend limit, after completing 16 of that round's 20 concept tasks and some of
its 20 papers -- leaving 4 papers pending and the round's other 36 completions
sitting un-rendered. Verified nothing was corrupted (just an unfinished round),
read and completed the remaining 4 papers by hand (Select2Reason, Budget-Aware
Anytime Reasoning, DREAM, and Follow the Path/fs1 -- all from actual PDFs), then
rendered the full round of 40 (20 papers, 20 concepts) together.

Also raised `config/topics/overthinking.yaml`'s `min_score` from 0.35 to 0.6
this session (a "must-read" bar, calibrated against the archive's own scoring
history to keep roughly the top 4-5% instead of the top 33%) -- see that
file's inline comment for the reasoning. A collection dry-run under the new
threshold found 55 of 735 candidates would clear it, versus 381 under the old
one.

Render surfaced 15 new near-duplicate concept-spelling pairs (e.g.
`llama-3-1-8b-instruct`/`llama3-1-8b-instruct`, `omni-math`/`omnimath`,
`qwen2-5-vl-3b`/`qwen2-5vl-3b`) on top of the 6 flagged in round 3 -- all
logged by `render` itself, none merged; still a person's call via
`config/concept-aliases.yaml`. Wiki grew 3790 to 4950 entities, 352 to 456
notes. `summaries_unread` is now 43, down sharply from ~123 before this round
-- partly from genuine draining, partly because papers admitted before
tonight's `min_score` change are a fixed, now-shrinking pool.

Queue refilled to 40 again (20 papers, 20 concepts) immediately after render,
per the usual cap-driven admission pattern. Left pending for next time.


Round 8 of the same drain: took the 20 papers + 20 concepts left pending
above. All 14 PDF-backed ACL Findings papers were read from the actual
document (backfill found no new PDFs to fetch this round); the remaining 6
were ICML/ICLR 2026 poster listings with empty abstracts in the payload,
resolved by fetching each paper's own conference page rather than guessing
from the title (`read_from: abstract` recorded honestly for those). Four of
the six turned out genuinely off-topic beyond a shared keyword (tabular
foundation models, image-captioning early exit, a pretraining generalization
theory paper, deep-RL actor-network early exit) and are flagged as such in
their `relevance` field; SPARC (VLM perception/reasoning separation for
test-time scaling) is the one borderline-relevant exception, noted as
indirectly relevant.

Several of this round's ACL papers are core, on-topic results: Step Pruner
(step-count reward instead of token-count, with an explicit account of the
step-merging reward-hacking failure mode token-penalty RL is prone to),
O1-Pruner (the "length disharmony" finding that other papers in this archive
already cite as a baseline), When Internalization Fails (a methodological
warning that first-k-token truncation baselines can badly overstate
efficiency gains because models compensate with longer post-think text), and
SCOPE (an exact token-level decomposition of GRPO entropy collapse, showing
a small ~5% subset of "branch-defining" tokens drives global exploration
loss). DeepPrune extends the archive's overthinking measurement to the
*parallel*-sampling setting: over 80% of parallel reasoning traces on
average converge to the same answer, a form of redundancy distinct from
single-trace length.

Render folded all 40 in. Wiki grew 4950 to 5181 entities, 456 to 465 notes.
`summaries_unread` dropped to 23 (from 43), the lowest this drain has been --
partly genuine progress, partly the shrinking pool of papers admitted before
the `min_score` change. Queue self-refilled to 40 (20 papers, 20 concepts)
immediately after render, per the same cap-driven pattern as every prior
round.


Round 9: took the 20 papers + 20 concepts left pending above. Backfill found
no new PDFs (0 candidates); all 20 papers this round were ICML/ICLR 2026
poster listings with empty abstracts, resolved via each paper's own
conference page (`read_from: abstract` for all 20). Most turned out
off-topic or only indirectly relevant -- domains ranging from visual
tracking, sparse autoencoders, mixed-integer optimization, tinyML
uncertainty quantification, and gradient-descent learning theory, matched
to the topic only by shared keywords (early exit, compute-optimal,
adaptive compression). Two are worth flagging as genuinely on-topic: Latent
Exploration Decoding (LED) identifies "exploration collapse" specifically at
the final output layer after RL post-training -- pass@n stops improving with
temperature because final-layer entropy collapses even though intermediate
layers stay diverse -- and fixes it training-free by decoding from
aggregated intermediate-layer posteriors instead, a direct complement to
this round's SCOPE reading (which found the same collapse driven by a small
token subset, at the training-dynamics level rather than the decoding
level). Smaller, Weaker, Yet Better is a compute-optimal-under-fixed-budget
result: training data from a cheaper, weaker model beats data from an
expensive, stronger one at matched inference cost, across three training
paradigms.

Render folded all 40 in. Wiki grew 5181 to 5286 entities, 465 to 466 notes.
`summaries_unread` dropped to 3 -- the lowest this drain has reached, and
close enough to call the immediate backlog effectively cleared (the
remaining 3 are presumably papers admitted in the last render's pass, not
yet queued). The queue refilled with 3 papers and 32 concept-definition
tasks (35 pending) rather than the usual 20/20 split, since fewer papers
remained to admit and more concepts crossed the promotion threshold on this
round's evidence. More near-duplicate concept spellings surfaced (gemini-2-5-
flash/gemini2-5-flash, llama-3-2-3b-instruct/llama3-2-3b-instruct,
longbench-v2/longbenchv2), added to the running list from earlier rounds --
still unmerged, still a person's call via `config/concept-aliases.yaml`.
