<!-- Generated from data/. Do not edit by hand: edits are overwritten on the next render. Put hand-written notes in the wiki instead. -->

# Research digest — 2026-09-03

0 new paper(s), 0 new recording(s).

Nothing matched today's filters.

## Queue

- pending: 5
- completed, awaiting render: 0
- applied: 950

## Run problems

- abstracts: all 6 lookup(s) failed: None

Worked through five items that had been left for a person.

**1. The version difference is now a finding** (`finding:d0e1f008559d4efc`), with
the venue's own listing recorded as a reference (`web:30075afa1472259a`). The
statement is about what the two documents contain, not about why: arXiv v2 ends
its baselines sentence '...and DEER; we also report S-GRPO, which requires
additional LLM training', carries S-GRPO rows in Table 1, and concedes that
RCPD 'outperforms S-GRPO on GPQA-D despite underperforming on AIME24'; the
camera-ready ends at 'and DEER', drops the rows, and drops the concession. The
string occurs 8 times in the preprint and once in the camera-ready. Dropping a
training-based method from an inference-time comparison is defensible, so the
finding does not impute a motive — it records that an evaluation built on the
published version alone overstates how RCPD stands.

**2. The stopping-criterion comparison now lives in
`wiki/concepts/answer-convergence.md`**, below the marker. It carries no
`analysis-sources` count, and says why: that counter checks against *that
note's* evidence, which is the three papers using the term, while the section
rests on about fifteen readings filed under other entities. The overthinking
note made the same call for the same reason.

**3. The 'bad cross-aliases' were not a new defect.** Both this session and a
subagent raised `llama-3-1-8b` and `llama-3-1-8b-instruct` each carrying the
other's name as an alias, as though it were an unhandled hazard. It is handled,
and was handled before: `docs/solved/concept-alias-candidates.md` records the
same failure on a different pair (`Llama-3.3-70B-Instruct` declared an alias of
`llama-3-1-70b`) and treats it as *the argument for the whole design* — the
`aliases` field is harvested and untrusted, the authored map is what merges.
That document also already rules that base/instruct pairs stay separate, as a
ruling and not a deferral. Nothing was changed. The correction worth carrying
forward is procedural: check `docs/solved/` before filing a defect, because the
investigation that would justify it may already exist.

**4. Fragmentation is at zero.** The 26 `variant` pairs render has been
reporting every pass were ruled in `config/concept-aliases.yaml` and applied.
All 26 are identical once non-alphanumerics are dropped, none is a base/instruct
pair, and the dry run showed the cost precisely: 26 records retired to
`data/concepts/retired/`, exactly one definition cleared —
`llama-3-1-8b-instruct`, which re-derives against 18 sources rather than the 14
it was written against. `fragmented` 26 -> 0; entities 5420 -> 5394; notes 479
-> 488, because merged entities crossed the promotion threshold. Committed as
an archive change rather than a code change, following the precedent set when
`min_score` moved: this file is a record of editorial decisions, not a change
to the program.

**5. The transcription audit found one real error in 232 readings.** Every
document-backed reading with a number-dense `results` field was screened by
extracting its numbers and checking each against its own PDF: **40 of 5,978
numbers (0.7%) appear nowhere in the source they cite.** Most of those 40 are
computed differences rather than table cells — REFRAIN's '+6.66pp' is a
subtraction, not a value anyone printed. One is a genuine defect:
`doi-10-18653-v1-2026-findings-acl-1717` reports ChartQA 70.68 and ScienceQA
71.99, and neither string occurs in the document under any of three extraction
modes; the nearest printed values are 70.40 and 71.82. That reading is archived
and cannot be reopened. It is not load-bearing for anything here.

**State plainly what this audit does not cover.** It detects numbers that are
absent from the source. It cannot detect the failure that prompted it — a real
number taken from the wrong row, or a ratio reported as an accuracy, which is
what the RCP reading did. Those values are present in the document and pass
every check here. Catching them needs adjacency between a method name and its
value, which was not built. So 0.7% bounds one failure mode and says nothing
about the other.

The merge queued 11 new definition tasks for the entities it consolidated; those
and the five-task refresh tail are pending.
