# Core pipeline. Everything else is Python standard library.
PyYAML>=6.0

# Optional extras
# ---------------
# YouTube transcripts. Without it, videos are archived without a transcript
# and flagged accordingly.
#   pip install youtube-transcript-api
