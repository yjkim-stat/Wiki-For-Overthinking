# group-relative advantage

<!-- auto:begin -->

Centring and scaling each rollout's reward by the mean and standard deviation of its own group, which removes the need for a learned critic and is what makes critic-free RL practical. All three sources treat its consequences rather than its definition, and between them they give the geometry. What the optimiser sees is the relative spacing of rewards within the group, not their values: a linear rescaling of the reward is cancelled by the normalisation and a nonlinear bounded one is not, so an unbounded reward lets a single outlier inflate the group standard deviation and flatten the advantages of every near-optimal sibling. Under binary rewards it is equivalent to optimising a specific transform of pass probability whose weight function is one over the square root of p(1-p), diverging at both ends, so gradient budget concentrates on prompts already solved -- measured directly at 36.4 percent of one benchmark's token-level budget on prompts above 0.9 pass rate. And a group whose members all score alike has zero advantage for every member and contributes nothing, which is 63 to 68 percent of groups in one study. The archive's reading is that group normalisation is not a neutral convenience: it fixes what the reward can express and where the gradient goes.

- **Kind**: concept
- **Also called**: group-relative normalization
- **Topics**: [reasoning-training](../topics/reasoning-training.md)
- **Sources**: 3

**Related**: [advantage estimation](advantage-estimation.md), [chain-of-thought prompting](../methods/chain-of-thought-prompting.md), [credit assignment](credit-assignment.md), [DAPO](../methods/dapo.md), [data efficiency](data-efficiency.md), [difficulty stratification](../methods/difficulty-stratification.md), [Dr. GRPO](../methods/dr-grpo.md), [exponential tilting](../methods/exponential-tilting.md), [GiGPO](../methods/gigpo.md), [GPQA](../datasets/gpqa.md), [GPT-5.5](../models/gpt-5-5.md), [GRPO](../methods/grpo.md), [GSM8K](../datasets/gsm8k.md), [HumanEval+](../datasets/humaneval.md), [Kimi-K2.6](../models/kimi-k2-6.md), [LiveCodeBench](../datasets/livecodebench.md), [long-horizon agency](long-horizon-agency.md), [MaxRL](../methods/maxrl.md), [MBPP+](../datasets/mbpp.md), [MMLU](../datasets/mmlu.md), [on-policy distillation](../methods/on-policy-distillation.md), [outcome reward](outcome-reward.md), [PPO](../methods/ppo.md), [prompt difficulty](prompt-difficulty.md), [Qwen2.5-Coder-7B](../models/qwen2-5-coder-7b.md), [Qwen3.5-4B](../models/qwen3-5-4b.md), [Qwen3.6-27B](../models/qwen3-6-27b.md), [REINFORCE](../methods/reinforce.md), [rejection sampling](../methods/rejection-sampling.md), [Skywork-OR1](../models/skywork-or1.md), [supervised fine-tuning](../methods/supervised-fine-tuning.md), [verifiable reward](verifiable-reward.md), [zero-advantage group](zero-advantage-group.md)

## What we have settled

- **Established** — Under group-relative optimisation the shape of the reward is a more effective lever than the optimiser, because the normalisation cancels a linear rescaling and preserves a nonlinear one — so fixing a sparse or badly-scaled signal at the reward is not interchangeable with fixing it at the update rule, and the two must be compared before either is credited.
  - Group-relative methods standardise rewards within a rollout group, so what reaches the optimiser is the relative spacing of rewards and not their values. Two consequences follow and both are measured. A linear rescaling of the reward is cancelled by the normalisation and changes nothing, while a bounded nonlinear reshaping survives it -- which is why replacing an unbounded error gap with a reciprocal mapping matters: an unbounded reward lets one outlier rollout inflate the group standard deviation and compress the advantages of every near-optimal sibling toward each other, so the group stops distinguishing better from best exactly where the useful gradient is. The direct comparison is the sharpest evidence: standard group-relative training given that reciprocal mapping beats four published optimisation-level variants -- reward normalisation, sample filtering, sequence-level ratios, soft advantages -- each run with the naive unbounded reward. That comparison is deliberately asymmetric, as its authors state, so it establishes that the two kinds of fix are not interchangeable rather than that reward-level fixes dominate. Three further sources give the same asymmetry from other directions. A derivation of what the normalisation optimises shows the induced prompt weighting under binary rewards is one over the square root of p(1-p), diverging at both ends, with the realised waste measured at 36.4 percent of one benchmark's token-level gradient budget spent on prompts already above a 0.9 pass rate -- a property of the reward-to-advantage transform that no optimiser change addresses. A reward-granularity sweep under fixed optimisation settings is monotone from sparse to hierarchical to argument-factorised, while the same paper finds token-level weighting improving the group-relative method and severely destabilising a batch-normalised one, so a shaping choice cannot be evaluated apart from the optimiser it is fed to. And a rubric study finds a reward-shape change -- grading each criterion correct, missing or wrong rather than yes/no -- worth 2.25 points of medical macro-average while an optimiser-side intervention on the same system moves that average by 0.25. The counterweight, and the reason this is stated as an asymmetry rather than a ranking, is that a difficulty-dependent multiplier interacts badly with the same normalisation and weakens a penalty precisely where outcome variance is highest: reshaping the reward is powerful because the normalisation transmits it, which also means a poorly chosen shape is transmitted.

## Appears in

- [DiDPO: Diff-in-Diff Policy Optimization for Coding Agent Training](../../archive/papers/2026/arxiv-2608-07147/summary.md) — Assigns credit in coding-agent RL by splitting each code diff into sub-diffs, matching semantically similar sub-diffs across rollouts to form advantage groups, and projecting the resulting diff-level advantage back onto the tokens that produced it.
- [SoftmaxGRPO: Learning to Reason using Softmax Advantage Group Estimation](../../archive/papers/2026/arxiv-2608-09271/summary.md) — Replaces GRPO's z-score group normalisation with a temperature-scaled softmax over rewards, which keeps the induced prompt-difficulty weighting bounded as pass probability approaches one and turns the temperature into a dial between REINFORCE and maximum-likelihood weighting.
- [Miner: Mining Intrinsic Mastery for Data-Efficient RL in Large Reasoning Models](../../archive/papers/2026/doi-10-18653-v1-2026-acl-long-237/summary.md) — Addresses wasted rollouts in critic-free RL on prompts where every sampled rollout is already correct and the advantage estimate is therefore zero.

<!-- auto:end -->

## Notes

_Anything below the marker above is yours. It is never overwritten._
